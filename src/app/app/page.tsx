import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
export default async function Dashboard() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login"); const { data: memberships } = await supabase.from("business_users").select("role,business:businesses(name,currency_code)").eq("status", "active"); return <main className="auth"><a className="brand" href="/"><span>M</span>MfaroInvoice</a><section><p className="eyebrow">BUSINESS DASHBOARD</p><h1>Welcome back.</h1>{memberships?.length ? memberships.map((membership, index) => <p key={index}>Active membership: <strong>{membership.role}</strong></p>) : <p>Finish onboarding to create your first business.</p>}</section></main>; }
